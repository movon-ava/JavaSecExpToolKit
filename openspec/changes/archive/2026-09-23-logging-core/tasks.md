# Tasks

## 1. 文件名与保留策略

写入域：`src/util/**`（角色：主 agent / 共享内核）
- [x] 1.1 文件名拼装与反解：严格 ISO 日期，非本工具命名返回空；验证：`LogCheck` 命名断言通过
- [x] 1.2 保留策略：`today - (N-1)` 为界，保留天数归一为不小于 1；验证：天数边界断言通过
- [x] 1.3 目录解析：环境变量 > 配置项 > 用户目录默认值；验证：`LogCheck` 目录断言通过
- [x] 1.4 清理只认本工具命名的文件，且校验目标在日志目录内；验证：越界文件未被删除的断言通过

## 2. 记录通道

写入域：`src/util/**`（角色：主 agent / 共享内核）
- [x] 2.1 级别过滤：低于阈值不写；OFF 时全部不写；验证：`LogCheck` 级别断言通过
- [x] 2.2 落盘内容含时间戳、级别、线程名与异常栈；验证：读取当天文件断言通过
- [x] 2.3 跨天切换：日期推进后写入落到新文件，旧文件不再被追加；验证：`LogCheck` 跨天断言通过
- [x] 2.4 写入失败静默：目录不可写时记录动作不抛异常；验证：失败路径断言通过
- [x] 2.5 未捕获异常处理器：后台线程异常被记录；验证：`LogCheck` 处理器断言通过

## 3. 收尾

写入域：`README*.md`、`PROGRESS.md`、`AI_REPORT.md`、`docs/DESIGN-logging.md`（角色：主 agent）
- [x] 3.1 设计文档成文；验证：`docs/DESIGN-logging.md` 存在且含根因段
- [x] 3.2 中英 README 同步；验证：两处小节一一对应
- [x] 3.3 追加 AI 报告与进度；验证：两份文件含本轮改动
- [x] 3.4 构建并复验 JAR 时间晚于全部源文件；验证：`build.ps1` 输出
