# PROGRESS

本文件记录 `JavaSecExpToolKit` 的阶段进度。详细改动过程见 [AI_REPORT.md](AI_REPORT.md)。

## 当前状态

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| Fastjson 探测（识别 / 版本 / 期望类 / DNS 探针 / CEYE 确认） | 可用 | 默认精简报告，五个模式一屏可见（16 行） |
| 会话 Cookie 与登录拦截识别 | 可用 | 区分「未携带」与「已携带仍被拦」 |
| 代理抓包（明文 HTTP + 拦截改包） | 可用 | HTTPS 仅 CONNECT 透传，不解密 |
| 抓包转换与一键发送 | 可用 | 支持导出多种 Cookie / 请求格式，请求体一并带过去 |
| Shiro 漏洞利用（检测 / 爆破 / 生成链 / 命令回显） | 可用 | 具备利用性质，仅限授权目标 |
| 抓包格式直接探测 | 可用 | 裸 Cookie / Key: Value / JSON 均可；自动剔除 Content-Length 与 gzip |
| 配置页 | 可用 | 十四个分组覆盖全部可持久化参数 |
| 规格驱动开发（OpenSpec） | 可用 | `openspec/specs/traffic/capture-bridge` 已建立；`config.yaml` 承载跨角色约束 |
| 多 Agent 协同框架 | 可用 | `tools/agent.ps1` 可启动角色化 agent；隔离与并发已实测，详见 `docs/AGENT-RUNBOOK.md` |
| 多 Agent 职责说明 | 已建立 | `docs/AGENT-ROLES.md` 定义八个角色的职责、边界与协作顺序 |
| 写入域机械校验 | 可用 | `tools/agent.ps1` 提交前逐文件比对写入域，越界则撤回暂存 |
| Agent 会话超时 | 可用 | `tools/agent.ps1` 按角色默认超时（监督 60 分钟，其余 40–45 分钟），超时终止进程树并保留日志 |
| 会话停滞看护 | 可用 | `tools/watchdog.ps1` 读日志判定 `PROGRESSING`/`WAITING`/`STUCK`；长等待（实测静默 771 秒）不误杀，真卡死终止并保留现场 |
| 工具链自检 | 可用 | `tools/check_agent_tools.ps1` 91 项机械断言，已用变体验证能报失败 |
| 单终端并发 | 可用 | `Start-Process -WindowStyle Hidden` 后台并发多角色；实测杀一个不影响另一个，主仓库不被污染 |
| 单条派发 | 可用 | `tools/dispatch.ps1` 由任务推断角色（无命中/多命中直接报错），跑完自动合并并清理；越界/超时/卡死一律不合并、保留现场 |
| 任务自动拆解编排 | 可用 | `tools/orchestrate.ps1 -Goal "..."` 一句话目标：只读会话拆解成 JSON 计划 → 机械校验 → 按依赖与写入域分批派发 → 按结果自动合并 → 逐步汇报；端到端实测 4 步 3 批全绿 |
| 派发汇报（角色与产出） | 可用 | 每次派发结束按角色聚合汇报：调用了哪些角色、各自几步、主要工作与产出文件；产出文件区间取 `<合并提交>^1..<合并提交>`，并行批次互不串号 |
| 依赖边界（解耦） | 可用 | 通用链引擎已与 Shiro 解耦；边界规则由 `tests/test_decoupling.py` 机械校验；新增「功能层叶子」档（`proxy` / `analyzer` 只可依赖共享内核） |
| Payload 生成（JavaChains Generate） | 可用 | `主页 → Payload → Payload 生成`：列式选链（点候选即追加并展开下一列，点回前面某列即从那里重开链）、列内关键字过滤、参数侧栏、生成 / 复制 / 导出 / 填入抓包页；429 节点、28 载体，仅本地内存构建 |
| 汇报角色中文名 | 可用 | 派发与编排汇报显示「主 agent / 探测 agent / 测试 agent」等；标识仍用于分支名、写入域与 `-Role` 参数 |
| 载荷生成自检 | 可用 | `tests/PayloadCheck.java` 100 条断言：目录 / 分组 / 导航 / 节点显示名 / 逐个载体 / 失败路径 / 双形态 / 安全，外加带外 Jar 25 种组合与 toString 触发节点归属 |
| toString 链（触发节点单独成页） | 可用 | `主页 → Payload → toString 链`：触发 / 中继 / 字节码尾段分段可选，自定义目标类、模板一键复制；5 条实测可用模板；触发节点归属由 `ChainScope` 单点维护，通用页候选同步剔除 |
| HTTP 带外 Jar | 可用 | `主页 → Payload → HTTP 带外 Jar`：5 种 Jar 包装 × 5 种末端动作（25 种组合实测全通过，产物均为合法 Zip）；输入框跟随动作声明启用；`OobJarService` 同实例托管并给出可复制回连地址，退出前停止托管 |
| 恶意服务器（JNDI / HTTP / TCP / FakeMySQL / JRMP） | 可用 | `主页 → 服务 → 恶意服务器`：五类服务真实启停、载荷发布、可复制回连地址；退出前释放全部端口。`src/service/` 为唯一调用上游服务端适配器的包，无需 Spring |
| 预设链 | 可用 | `主页 → Payload → 预设链`：内置 52 条预设法（7 分类）、链步骤只读、输入按声明类型渲染、本地生成；可发到恶意服务器或抓包页 |
| 主窗口默认全屏 | 可用 | `MAXIMIZED_BOTH`，最小尺寸 1180×720，保证服务页四块区域可用 |
| 载荷编码缺陷修复 | 已修复 | 文本形态载体（Shiro）被双重 Base64 编码，导致生成的载荷无法被本机密钥解密；根因与修法见 AI_REPORT.md |
| 配置页（恶意服务器 / 预设链） | 可用 | 新增「恶意服务器配置」（绑定 / 公布地址 + 七类端口）与「预设链配置」（默认分类），保存后即刻下发到已打开页面 |
| 配置页（toString 链 / 带外 Jar） | 可用 | 新增「toString 链配置」（默认模板 / 默认命令）与「带外 Jar 配置」（绑定地址 / 端口 50001 / 默认 URL / 默认路径 / 默认执行参数），共 7 个配置键 |
| `Main.java` 模块化 | 已完成 | 1757 → 327 行，仅剩装配与切页；六套 Java 自检与 106 项 Python 测试全绿，断言一条未改 |
| 界面自检稳定门面 | 可用 | `ui/UiHandle` 按名字取控件、`ui/WidgetRegistry` 登记清单；自检不再依赖控件声明在哪个类 |
| 自检副作用清理 | 可用 | `tests/TestProcessGuard.java` 八套入口全接入：启动时给现存计算器进程拍快照，退出钩子只强制结束快照之后新起的进程；`tests/test_selfcheck_hygiene.py` 5 项断言守住接入与顺序 |
| 界面层解耦（已完成） | 已完成 | `PayloadPage` 787→252、`PayloadController` 739→546 行；拆出 `PayloadPanels` / `PayloadColumns` / `PayloadOutputText` / `PayloadExporter` / `ChainColumnFilter` / `ChainNodeRenderer` 等协作类，界面文件全部 ≤600 行 |
| 启动预热 | 可用 | `src/ui/StartupWarmup.java`：引擎 / 预设链 / 服务适配器一次性预热（实测首次 1263 ms，二次 0 ms，预设 52 条），幂等且不阻塞事件分发线程；配无边框启动画面 |
| 选链区比例对齐网页版 | 可用 | 用 CDP 实测上游 `#/Generate/<payload>`：控制台 360px、选链区 486px；`CONSOLE_WEIGHT` 由实测常量算出，选链区占两块之和约 57% |
| 分割线布局期生效 | 已修复 | 新增 `RatioSplitPane`，比例在 `doLayout()` 里校正；修掉异步组件事件留下的空窗（同一构建曾读到 101px 与 133px 两个值） |
| 界面遮挡错位排查 | 可用 | 新增布局审计：八页扫描，`TOTAL: 0`（无越界、无重叠、无被裁控件） |
| 配置页补漏 | 可用 | 「Payload 生成配置」补上 `生成后默认展开完整载荷` 与 `默认开启悬停选链` 两个开关，配置键 `payload_auto_expand` / `payload_hover_select` 同时写入 `config.properties` |
| 小工具 - 文件上传 | 可用 | `主页 → 小工具 → 文件上传`：选本地文件按 multipart 上传，记录状态码 / 耗时 / 响应头 / 响应体 / Set-Cookie；一次请求不跟随跳转；单文件上限 8 MB；失败路径全部给可读结论 |
| 文件上传请求体通道 | 已建立 | 引擎新增字节请求体入口（文本入口签名不变），文件按原始字节进入 multipart 分段，`Content-Type` 的 boundary 与请求体始终一致 |
| 小工具配置 | 可用 | 新增「小工具配置」分组：默认上传 URL、默认表单字段名、上传超时（默认 30 秒） |
| 漏洞分析（组件与漏洞） | 可用 | `主页 → 漏洞分析 → 组件与漏洞`：本地读 `pom.properties` / `pom.xml` / `MANIFEST` + 文件名推断，按 25 条内置规则判定已知漏洞；结论带来源、依据、可信度与跳转按钮；实测单 jar 30 ms |
| 漏洞分析（调用链查询） | 可用 | `主页 → 漏洞分析 → 调用链查询`：外部 `jar-analyzer-engine` 建库后按 Sink 命中 / 入口点 / 字符串常量 / 组件清单 / 总览查询；超时终止进程树并清临时目录 |
| 漏洞分析（反编译） | 可用 | 用运行期依赖自带的 CFR 反编译，零新增依赖、无需 Node；实测 84 个类 3.5 秒；产物只写不读 |
| 源码级别门禁 | 可用 | `tests/test_java8_source_level.py`：src/ 不得出现 Java 9+ API 与语法。此前自检用的 `javac` 不带 `--release 8`，9+ API 只在 Maven 构建时才报错（本轮踩到 `Process#descendants`），现改为机械断言并可被变体验证 |
| 日志记录 | 可用 | `src/util/Log.java`（记录通道）+ `src/util/LogFiles.java`（文件名与保留策略）：按日期分文件 `app-YYYY-MM-DD.log`，默认保留 7 天（含今天），启动与跨天各清理一次；级别四档、默认开启、目录可由环境变量 `JSETK_LOG_DIR` 覆盖；新增「日志配置」分组；未捕获异常统一入库 |

## 已完成阶段

- 2026-09-18 — 工程搭建、探测引擎雏形、Swing 界面、Maven 构建
- 2026-09-18 — 五模式探测（识别 / 版本 / 期望类 / DNS 探针 / CEYE 确认）
- 2026-09-19 — 探测准确性修复与结果可读性改造
- 2026-09-19 — 抓包与格式转换、代理抓包与拦截改包、Shiro 模块与 java-chains 集成
- 2026-09-19 — 抓包后一键发送到其它功能；登录拦截识别与会话 Cookie
- 2026-09-19 — README 中英双语，推送至远端仓库
- 2026-09-20 — 配置页补全新增功能配置；Fastjson 精简报告与一屏显示
- 2026-09-20 — 抓包格式直接可探测：请求头容错解析与净化，观察模式下可导出抓包
- 2026-09-21 — 引入 OpenSpec 规格驱动开发，设计多 Agent 协同框架、Java 模块化方案与 Payload 生成方案
- 2026-09-21 — 链引擎解耦：Base64 下沉共享内核，依赖边界自检落地，多 Agent 职责文档成文
- 2026-09-21 — 多 Agent 并发工具链：角色化启动脚本、写入域机械校验、运行手册成文
- 2026-09-21 — Agent 会话超时上限：按角色默认超时，超时终止进程树并保留现场
- 2026-09-21 — 看护 agent：按日志内容区分长等待与卡死，修复开关失效与变量同名两个真实缺陷
- 2026-09-21 — 多 Agent 落地实测：单终端并发可行、派发权在脚本而非主 agent、监督为阶段末只读审计
- 2026-09-21 — 主 agent 自动拆解编排：一句话目标拆解分批派发、自动合并与汇报；修掉「防呆分支成死代码」与「共享内核无角色可写」两个真实缺陷
- 2026-09-21 — 派发汇报补角色维度：列出调用的角色与各自主要工作、产出文件；修掉并行批次产出文件串号
- 2026-09-21 — 汇报角色中文化；JavaChains 可视化落地：通用引擎下沉独立包、Shiro 侧改为委派、配置落进配置页；修掉输出区被压成细线（根因：分栏缺最小高度）
- 2026-09-21 — 恶意服务器与预设链落地：新增 `src/service/`、`src/preset/` 两个包与两个页面；导航重组为网页版顺序、主窗口默认全屏；修掉载荷双重 Base64 编码（根因：字节分支恒真）
- 2026-09-21 — 界面自检加固：导航断言改为按展开状态现算项数，新增预设链 / 服务页 / 配置页断言
- 2026-09-21 — `Main.java` 模块化落地：抽出 10 个控制器 / 表单 / 门面类，导航、探测、抓包、Shiro、代理、配置各自独立；自检改走 `ui/UiHandle` 稳定门面，132 处字段访问与 3 个方法调用断言一条未改
- 2026-09-21 — Payload 生成页对齐 JavaChains Generate：列式选链替代「下拉 + 追加按钮」，载体并入第一列，新增节点显示名只读查询；修掉「点候选无反应」（根因：追加被边界判断挡掉）与过滤时光标丢失
- 2026-09-21 — 收尾：构建复验（JAR 时间晚于 61 个源文件）、清理仓库内 18 字节不可达损坏 tree 使 `git fetch` 恢复、本轮改动提交推送
- 2026-09-22 — 自检副作用清理：六套 Java 自检接入计算器进程守卫，退出时只关掉构建期新弹出的计算器（实测 2 个）
- 2026-09-22 — 小工具 - 文件上传：新增「小工具」一级分类与 multipart 上传（字节级请求体通道 + 20 余条界面断言 + 8 项引擎单测），新增「小工具配置」分组；OpenSpec change `tools-file-upload` 已归档，主 spec 落地 `openspec/specs/tools/file-upload`
- 2026-09-22 — 界面层解耦达标：Payload 三件套按规格拆到 600 行以内，新增 7 个协作类；自检全绿
- 2026-09-22 — 配置页补漏：Payload 生成配置的「默认展开」与「悬停选链」两个开关落页落键
- 2026-09-22 — 小工具模块新增「文件上传」：选本地文件按 multipart 上传到指定接口并保留原始响应，配置键 `upload_url` / `upload_field` / `upload_timeout` 落进配置页
- 2026-09-22 — toString 利用链单独成页：补上漏掉的触发节点（根因）后 5 条模板全部可用；新增 `ChainScope` 单点维护触发节点归属，通用生成页候选同步剔除
- 2026-09-22 — HTTP 带外 Jar 落地：`JarPreset` 5 种包装 × 5 种末端动作，25 种组合实测全通过；新增 `OobJarService` 把「同实例托管」约束收在类内（根因：另一个实例发布会让地址回落到 50000）
- 2026-09-22 — 启动预热与版面比例对齐：一次性初始化挪到启动阶段（幂等，二次 0 ms）；用 CDP 实测网页版比例对齐选链区；修掉分割线靠异步事件补算留下的空窗
- 2026-09-22 — 漏洞分析一级目录：本地依赖坐标（`pom.properties > pom.xml > MANIFEST > 文件名`）打底 + 25 条内置规则判定，外部引擎调用链查询深挖，内置 CFR 反编译定点确认；新增 `src/analyzer/`（叶子层）与 `src/analyze/`（编排层）两个包、`python/jar_report.py` 与「漏洞分析配置」分组；修掉 `pom.properties` 键大小写与小写常量不匹配导致整份清单静默退化、版本比较缺段语义让预发布版倒挂两个真实缺陷
- 2026-09-22 — 本构建暴露并修掉第四个缺陷（只在 Maven 构建下现形）：`src/` 用了 Java 9 的 `Process#descendants()` 而源码级别锁在 release 8，七套自检因 `javac` 不带 `--release 8` 而全绿；抽出反射降级的 `ProcessTree` 并新增 Java 8 源码级别门禁，已用变体验证门禁能抓到原缺陷
- 2026-09-23 — 日志记录：`src/util/Log`（记录通道）+ `src/util/LogFiles`（文件名解析与保留策略）+ `src/ui/LoggingBootstrap`（配置语义）落地，按日期分文件、默认保留 7 天、启动与跨天各清理一次；代理 / 探测 / 引擎 / 分析 / 预热等静默失败路径全部接入；新增「日志配置」分组 5 键与 `LogCheck` 79 条断言；已归档三个 change（`logging-core` / `logging-integration` / `ui-logging-config`）并落地三份主 spec

## 后续可做

- `version` 模式仍有 `1.2.70-1.2.72` 与 `1.2.73-1.2.80` 无法细分的盲区。
- 代理抓包不记录 HTTPS 明文；记录上限 500 条且不保留历史列表。
- 登录拦截识别依赖网页关键词，JSON 响应或纯状态码表达的未登录仍走传输层失败提示。
- `Main.java` 已降到 327 行（≤400 上限）；`ui` 包内文件平铺，若继续增长可再按功能分子包（需同步补 `ALLOWED_EDGES`）。
- 代理拦截等待上限（120 秒）仍是硬编码常量，未进配置页。
- 预设链里出现分叉的链仍按线性顺序构建；分支型链尚未暴露到界面。
- 恶意服务器不含 LDAPS 证书：需要 HTTPS 回调时须自行准备 JKS 后再启用该端口。
- 界面自检必须按 `run.ps1` 的方式运行（带两个 `--add-opens`），否则预设链生成会失败。
- Payload 生成已落地通用引擎与界面；change 规划件 `payload-generation` 的 36 个任务尚未逐条勾选归档，
  待功能稳定后再走 archive 流程。
- Payload 页只覆盖「载体 + 线性链」；上游的分支型链（一个节点接多个后继后再汇合）尚未暴露到界面。
- 载荷可用性取决于目标依赖版本：本工具只保证「引擎认可这条链并能构建出载荷」，不保证目标触发。
- 现有 UI 自检已改走 `ui/UiHandle`；再新增断言时只需在 `WidgetRegistry` 里登记控件名。
- 计算器守卫用 `ProcessHandle` 按「进程名 + 启动时间晚于快照」判定，替代程序（如把 `calc` 换成别的命令）不会被清理；
  若上游默认参数值再变化，需要同步调整 `TestProcessGuard` 的进程名匹配。
- Payload 页列宽按容器宽度在 300~500px 之间自适应、放不下时横向滚动；显示名过长时在列内截断成省略号，
  完整标识靠悬停提示查看。
- 选链区比例取自上游 beta4 的实测值（360 / 486）；上游改版后需重新量测并更新常量。
- `RatioSplitPane` 的比例在被用户拖动后即失效，窗口缩放不再回到 57%——刻意优先尊重使用者调好的位置。
- Payload 页列数 = 链长 + 1，末端无后继时不展开空列；分支型链（一个节点接多个后继后再汇合）仍未支持。
- 节点显示名取自上游元数据：429 个节点里 3 个没有显示名，界面对这类节点回退展示标识。
- 漏洞分析的内置规则只覆盖 25 条，且只收录「判定后能接上本工具后续动作」的组件；
  未命中不等于目标没有漏洞（报告里固定带这句免责说明）。
- 调用链分析需要使用者自备 `jar-analyzer-engine` 的 jar；未配置时该页只跑本地分析。
  本机未持有该 jar，因此引擎调用路径止于「命令行拼装 + 失败路径 + 数据库查询」三层验证，
  数据库查询用模拟库（`tests/test_jar_report.py`）覆盖。
- CFR 是同步 API，反编译超预算时**无法中途打断**，只能完成后标注；产物仍保留。
- `jar-analyzer/jsd`（Node 版反编译器）只保留了命令行拼装能力，未接入界面，也未释放进 JAR。
- 本地依赖分析对「被 shade 重打包且未保留 `META-INF/maven`」的 jar 只能退化到文件名推断，
  此时结论可信度标为「低」。
- 源码级别门禁（`tests/test_java8_source_level.py`）用的是显式黑名单，不是全量 API 比对；
  能挡住已知的高频 Java 9+ API，但不保证穷尽。
- `ProcessTree` 在 Java 8 运行时只能强杀父进程，收不掉孙进程（Java 8 无等价能力）。
- 日志只记录事件与结论，**不含请求体、响应体、密钥与令牌**；需要看报文原文时仍要看抓包页。
- 日志清理只认「本工具命名的文件」（`app-YYYY-MM-DD.log` 且位于日志目录下），
  因此同目录里手工放的其它文件不会被删；反过来说，手工改名的旧日志不会被自动清掉。
- 日志目录不可写时**静默失败**（记录动作全部生效但落不了盘），不影响任何功能；
  需要日志时用环境变量 `JSETK_LOG_DIR` 指向可写目录。
- `preset` 包保持零出边：读取内置预设失败时不做任何记录，只把失败交给界面 `loadError()` 展示。
- 尚未归档的 change：`analyze-local-deps`、`payload-generation`、`java-chains-workbench`。
