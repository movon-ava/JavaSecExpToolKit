# 行尾统一为 LF。系统/用户的 core.autocrlf 默认会按平台改写行尾，
# 使同一提交在不同机器上检出不同字节，进而影响打包产物。
* text=auto eol=lf

# jar 是二进制；第三方依赖 java-chains 2.0.0-beta4 单包约 176 MB，
# 超出常规仓库单文件上限，改走 Git LFS。
*.jar binary filter=lfs diff=lfs merge=lfs -text
