# 构建产物：由 build.ps1 / mvn -f src\pom.xml clean package 生成，不入版本库
target/
JavaSecExpToolKit.jar

# 运行时依赖目录：构建时由 Maven 从 libs-repo/ 复制而来，可重新生成。
# 必须锚定到仓库根目录：写成 lib/ 会连带忽略 tools/lib/（工具链源码），
# 导致 tools/lib/CodexCli.ps1 无法入库。
/lib/

# 本地修改快照：只用于回滚，保留最近三次即可，不入版本库
.backups/

# java-chains 运行期缓存：正常落在 lib/chains-config/；依赖目录缺失时会回落到工作目录
chains-config/

# Python 运行期生成物
**/__pycache__/
*.pyc

# 其它 AI 工具在本仓库留下的本地会话目录：与项目无关，不入版本库
.pi/
